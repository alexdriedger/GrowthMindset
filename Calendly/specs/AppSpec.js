export default function (spec) {
  spec.describe('Creating Meeting', () => {
    spec.it('works', async () => {
      // await spec.press('HomeScreen.CreateMeeting');
      // await spec.exists('AvailabilityForm');
      // await spec.press('AvailabilityForm.Submit');
      // await spec.exists('AvailabilityConfirmScreen');
    });
  });
}
